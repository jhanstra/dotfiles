# Linked to ~/.gitconfig.local by the Headway setup adapter.

# Keep Azure DevOps credentials separate for each organization/repository URL
[credential "https://dev.azure.com"]
	useHttpPath = true

[user]
	name = jhanstra
	email = jared.hanstra@findheadway.com
